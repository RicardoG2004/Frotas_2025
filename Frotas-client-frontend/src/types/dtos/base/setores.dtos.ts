export interface CreateSetorDTO {
    Descricao: string;
}

export interface UpdateSetorDTO {
    Descricao: string;
}

export interface SetorDTO {
    id: string;
    descricao: string;
    createdOn: Date;
}

